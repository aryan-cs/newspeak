# Validator Gold Set Card

Status: seed development examples only.

Path: `data/eval_sets/validator_gold/examples.jsonl`

Purpose: exercise validator-gold evaluation tooling before a reviewed and
adjudicated gold set exists.

Limitations:

- These examples are hand-authored development checks.
- They are not a final gold set.
- They must not be used to claim validator deployment precision or recall.

Before primary compliance reporting, replace or extend this seed set with a
reviewed validator-gold set with annotation batches and adjudication notes.
