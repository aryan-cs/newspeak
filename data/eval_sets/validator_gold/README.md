# Validator Gold Set

Gold-labeled validator examples belong here after review and adjudication.

Required labels:

- `text`
- `target` (`dialect` or `structured_simple`)
- `expected_valid`
- `violation_types`
- `notes`
- `annotator_batch_id`

Do not treat unit-test examples as the final validator gold set.
